---
phase: 15-pre-flight-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stream/opt/allocation/constraint_optimization/transfer_and_tensor_allocation.py
  - stream/api.py
  - main_gemm.py
  - main_swiglu.py
  - main_swiglu_dse.py
  - main_swiglu_dse_single.py
  - main_aie_co.py
  - main_aie_ga.py
  - main_aie_codegen_conv2d.py
  - main_stream_co.py
  - main_stream_ga.py
  - main_gemm_manual.py
  - tests/study_constraint_toggles.py
  - tests/study_swiglu_backends.py
  - tests/verify_backends.py
  - tests/study_constraint_toggles_cross_backend.py
autonomous: true
requirements:
  - CLEAN-02
  - CLEAN-04

must_haves:
  truths:
    - "Running a full TETRA solve with OR-Tools produces zero lines on stdout"
    - "An MCP server process can import stream.api without triggering any module-level basicConfig() side effects"
    - "CLI scripts (main_*.py) still produce the same logging output as before (configure_logging called at startup)"
  artifacts:
    - path: "stream/opt/allocation/constraint_optimization/transfer_and_tensor_allocation.py"
      provides: "Logger-based diagnostic output instead of print()"
      contains: "_logger.info"
    - path: "stream/api.py"
      provides: "configure_logging() helper function, no module-level basicConfig()"
      exports: ["configure_logging"]
  key_links:
    - from: "main_gemm.py"
      to: "stream/api.py"
      via: "configure_logging() call before pipeline use"
      pattern: "configure_logging"
    - from: "stream/opt/allocation/constraint_optimization/transfer_and_tensor_allocation.py"
      to: "logging module"
      via: "_logger.info/_logger.warning calls"
      pattern: "_logger\\.(info|warning)"
---

<objective>
Silence stdout pollution and decouple logging configuration so MCP stdio transport remains clean.

Purpose: MCP's stdio transport uses stdout exclusively for JSON-RPC messages. Any bare print() calls corrupt the stream. Module-level basicConfig() prevents MCP server from controlling its own logging. These are hard blockers for all MCP work in Phases 16-18.
Output: Zero print() calls in the MILP solve path; logging configured via explicit helper, not import side-effect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-pre-flight-cleanup/15-CONTEXT.md

@stream/opt/allocation/constraint_optimization/transfer_and_tensor_allocation.py
@stream/api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace print() calls in TTA with logger calls (CLEAN-02)</name>
  <files>stream/opt/allocation/constraint_optimization/transfer_and_tensor_allocation.py</files>
  <read_first>
    - stream/opt/allocation/constraint_optimization/transfer_and_tensor_allocation.py (lines 50-60 for existing logger, lines 1750-1755, 1900-1910, 2080-2085, 2150-2156 for print calls)
  </read_first>
  <action>
    Replace the 5 bare print() calls with logger calls using the existing `_logger` variable (already defined at line 55 as `_logger = logging.getLogger(__name__)`). Per D-01 and D-02:

    1. Line 1753: `print(f"Optimization metrics saved to {save_path}")` -> `_logger.info("Optimization metrics saved to %s", save_path)`
    2. Line 1906: `print(f"Slot latency breakdown saved to {save_path}")` -> `_logger.info("Slot latency breakdown saved to %s", save_path)`
    3. Line 1909: `print(f"[warn] save_slot_latency_breakdown failed: {e}")` -> `_logger.warning("save_slot_latency_breakdown failed: %s", e)` (per D-02: use logger.warning since it reports an error condition; drop the "[warn]" prefix since the log level already conveys severity)
    4. Line 2083: `print(f"Optimization progress plot saved to {save_path}")` -> `_logger.info("Optimization progress plot saved to %s", save_path)`
    5. Line 2155: `print(f"Optimization trace saved to {file_path}")` -> `_logger.info("Optimization trace saved to %s", file_path)`

    Use lazy %-formatting (not f-strings) in all logger calls per Python logging best practices.
  </action>
  <verify>
    <automated>cd /home/micas/stream_aie && grep -n "print(" stream/opt/allocation/constraint_optimization/transfer_and_tensor_allocation.py | grep -v "^#" | grep -v "# print" && echo "FAIL: print() calls still present" || echo "PASS: no bare print() calls"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "print(" stream/opt/allocation/constraint_optimization/transfer_and_tensor_allocation.py` returns 0 (no active print calls)
    - `grep -c "_logger.info" stream/opt/allocation/constraint_optimization/transfer_and_tensor_allocation.py` returns at least 4 (the 4 info replacements)
    - `grep -c "_logger.warning.*save_slot_latency_breakdown" stream/opt/allocation/constraint_optimization/transfer_and_tensor_allocation.py` returns 1 (the warning replacement per D-02)
    - `ruff check stream/opt/allocation/constraint_optimization/transfer_and_tensor_allocation.py` passes
  </acceptance_criteria>
  <done>All 5 print() calls in TTA replaced with _logger.info() or _logger.warning() calls. No bare print() remains in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Move module-level basicConfig to configure_logging helper (CLEAN-04)</name>
  <files>
    stream/api.py
    main_gemm.py
    main_swiglu.py
    main_swiglu_dse.py
    main_swiglu_dse_single.py
    main_aie_co.py
    main_aie_ga.py
    main_aie_codegen_conv2d.py
    main_stream_co.py
    main_stream_ga.py
    main_gemm_manual.py
    tests/study_constraint_toggles.py
    tests/study_swiglu_backends.py
    tests/verify_backends.py
    tests/study_constraint_toggles_cross_backend.py
  </files>
  <read_first>
    - stream/api.py (lines 1-30 for current basicConfig pattern)
    - main_gemm.py (full file, to see import pattern)
    - main_swiglu.py (full file)
    - main_aie_co.py (full file)
  </read_first>
  <action>
    Per D-06: Move the module-level `logging.basicConfig()` at api.py line 27 into a helper function.

    In stream/api.py:
    1. Keep lines 25-26 (`_logging_level` and `_logging_format`) as module-level constants (they define defaults)
    2. Replace line 27 (`_logging.basicConfig(level=_logging_level, format=_logging_format)`) with a function:
       ```python
       def configure_logging(level: int = _logging_level, fmt: str = _logging_format) -> None:
           """Configure root logging. Called by CLI scripts; MCP server manages its own logging."""
           _logging.basicConfig(level=level, format=fmt)
       ```
    3. Add `configure_logging` to the public exports if an `__all__` exists (there is none, so skip)

    In every main_*.py script and study/verify script that imports from stream.api:
    1. Add `configure_logging` to the existing import: `from stream.api import optimize_allocation_co, configure_logging` (or the equivalent for each script)
    2. Add `configure_logging()` as the FIRST line inside the `if __name__ == "__main__":` block (or at the top of the script's main execution path if there's no such guard)

    Scripts to update (all in repo root unless noted):
    - main_gemm.py
    - main_swiglu.py
    - main_swiglu_dse.py (imports optimize_mapping)
    - main_swiglu_dse_single.py
    - main_aie_co.py
    - main_aie_ga.py (imports optimize_allocation_ga)
    - main_aie_codegen_conv2d.py
    - main_stream_co.py
    - main_stream_ga.py (imports optimize_allocation_ga)
    - main_gemm_manual.py
    - tests/study_constraint_toggles.py
    - tests/study_swiglu_backends.py
    - tests/verify_backends.py
    - tests/study_constraint_toggles_cross_backend.py

    DO NOT add configure_logging() to test files in tests/unit/ or tests/integration/ — those are run by pytest which has its own logging configuration.
  </action>
  <verify>
    <automated>cd /home/micas/stream_aie && python -c "import stream.api; import sys; assert not any(h for h in __import__('logging').root.handlers), 'Root logger has handlers after import'" 2>&1 && echo "PASS: no basicConfig side-effect on import" || echo "CHECKING: may have pre-existing handlers"; ruff check stream/api.py main_gemm.py main_swiglu.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "basicConfig" stream/api.py` returns 1 (inside configure_logging function only)
    - `grep -n "def configure_logging" stream/api.py` returns a match
    - `grep -c "configure_logging" main_gemm.py` returns at least 1
    - `grep -c "configure_logging" main_swiglu.py` returns at least 1
    - No module-level `_logging.basicConfig(` call exists outside the function body in api.py
    - `ruff check stream/api.py` passes
    - `pytest tests/unit/ -x -q` passes (no regression from logging change)
  </acceptance_criteria>
  <done>Module-level basicConfig removed from api.py. configure_logging() helper defined. All CLI scripts and study scripts call configure_logging() explicitly. Importing stream.api no longer configures root logging.</done>
</task>

</tasks>

<verification>
1. `grep -rn "print(" stream/opt/allocation/constraint_optimization/transfer_and_tensor_allocation.py` returns no active print calls
2. `python -c "import stream.api"` does not produce any logging configuration side-effects
3. `ruff check stream/api.py stream/opt/allocation/constraint_optimization/transfer_and_tensor_allocation.py` passes
4. `pytest tests/unit/ -x -q` passes
</verification>

<success_criteria>
- Zero bare print() calls remain in the TTA file
- stream.api can be imported without triggering basicConfig()
- All CLI scripts still produce logging output when run directly (via configure_logging())
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-pre-flight-cleanup/15-01-SUMMARY.md`
</output>
