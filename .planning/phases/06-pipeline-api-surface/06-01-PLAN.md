---
phase: 06-pipeline-api-surface
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - stream/api.py
  - stream/stages/allocation/constraint_optimization_allocation.py
  - stream/cost_model/steady_state_scheduler.py
  - tests/unit/test_pipeline_threading.py
autonomous: true
requirements: [PIPE-01, UI-01]

must_haves:
  truths:
    - "optimize_allocation_co() accepts constraint_selection kwarg and places it in StageContext"
    - "optimize_mapping() accepts constraint_selection kwarg and places it in StageContext"
    - "ConstraintOptimizationAllocationStage reads constraint_selection from context and passes it to SteadyStateScheduler"
    - "SteadyStateScheduler stores constraint_selection and passes it to TransferAndTensorAllocator"
    - "Passing ConstraintSelection(memory_capacity=False) via optimize_allocation_co reaches TTA with memory_capacity=False"
  artifacts:
    - path: "stream/api.py"
      provides: "constraint_selection kwarg on optimize_allocation_co and optimize_mapping"
      contains: "constraint_selection: ConstraintSelection | None = None"
    - path: "stream/stages/allocation/constraint_optimization_allocation.py"
      provides: "Context reading and SteadyStateScheduler forwarding"
      contains: "constraint_selection"
    - path: "stream/cost_model/steady_state_scheduler.py"
      provides: "Storage and TTA forwarding"
      contains: "constraint_selection"
    - path: "tests/unit/test_pipeline_threading.py"
      provides: "Unit tests for pipeline threading and API surface"
      min_lines: 50
  key_links:
    - from: "stream/api.py"
      to: "StageContext.from_kwargs"
      via: "constraint_selection= kwarg in from_kwargs call"
      pattern: "constraint_selection=constraint_selection"
    - from: "stream/stages/allocation/constraint_optimization_allocation.py"
      to: "stream/cost_model/steady_state_scheduler.py"
      via: "constraint_selection kwarg on SteadyStateScheduler constructor"
      pattern: "constraint_selection=self.constraint_selection"
    - from: "stream/cost_model/steady_state_scheduler.py"
      to: "TransferAndTensorAllocator"
      via: "constraint_selection kwarg on TTA constructor in run()"
      pattern: "constraint_selection=self.constraint_selection"
---

<objective>
Thread `constraint_selection` parameter through the pipeline from the two public API functions (`optimize_allocation_co`, `optimize_mapping`) through `StageContext`, `ConstraintOptimizationAllocationStage`, and `SteadyStateScheduler` to reach `TransferAndTensorAllocator` (which already accepts it from Phase 5). Write unit tests proving the full threading path works.

Purpose: PIPE-01 and UI-01 require that a `ConstraintSelection` instance passed at the API level arrives at TTA unchanged. Without this wiring, CLI flags (Phase 6 Plan 02) would have no effect.
Output: Three modified production files + one new test file confirming end-to-end parameter threading.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pipeline-api-surface/06-RESEARCH.md
@.planning/phases/05-constraintselection-dataclass/05-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From stream/opt/solver/solver.py (Phase 5):
```python
@dataclass(frozen=True)
class ConstraintSelection:
    memory_capacity: bool = True
    object_fifo_depth: bool = True
    buffer_descriptors: bool = True
    dma_channels: bool = True
```

From stream/opt/solver/__init__.py:
```python
from stream.opt.solver.solver import ConstraintSelection  # already exported
```

From stream/api.py (current import line 11):
```python
from stream.opt.solver import GurobiBackend, SolverBackend
```

From stream/stages/allocation/constraint_optimization_allocation.py (current line 56):
```python
self.backend: str = self.ctx.get("backend", "ORTOOLS_GSCIP")
```

From stream/cost_model/steady_state_scheduler.py (current __init__ signature lines 56-66):
```python
class SteadyStateScheduler:
    def __init__(
        self,
        workload: Workload,
        accelerator: "Accelerator",
        mapping: Mapping,
        fusion_splits: dict[LayerDim, int],
        cost_lut: CoreCostLUT,
        nb_cols_to_use: int = 4,
        output_path: str = "",
        backend: str = "ORTOOLS_GSCIP",
    ):
```

From stream/cost_model/steady_state_scheduler.py (TTA instantiation lines 123-135):
```python
tta = TransferAndTensorAllocator(
    self.ssw,
    timeslots,
    accelerator=self.accelerator,
    iterations=self.iterations,
    ssis=self.ssis,
    multiplicities=multiplicities,
    mapping=self.mapping,
    cost_lut=self.cost_lut,
    nb_cols_to_use=self.nb_cols_to_use,
    output_path=self.output_path,
    backend=self.backend,
)
```

From transfer_and_tensor_allocation.py (already accepts, from Phase 5):
```python
# keyword-only after backend=
constraint_selection: ConstraintSelection | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread constraint_selection through pipeline (api.py, Stage, Scheduler) + tests</name>
  <files>
    stream/api.py,
    stream/stages/allocation/constraint_optimization_allocation.py,
    stream/cost_model/steady_state_scheduler.py,
    tests/unit/test_pipeline_threading.py
  </files>
  <read_first>
    stream/api.py,
    stream/stages/allocation/constraint_optimization_allocation.py,
    stream/cost_model/steady_state_scheduler.py,
    stream/opt/solver/solver.py,
    tests/unit/test_constraint_selection.py
  </read_first>
  <behavior>
    - Test 1 (test_api_optimize_allocation_co_accepts_constraint_selection): Calling optimize_allocation_co with constraint_selection=ConstraintSelection(memory_capacity=False) does not raise TypeError (signature accepts kwarg). Test via inspect.signature to verify the parameter exists and has default None.
    - Test 2 (test_api_optimize_mapping_accepts_constraint_selection): Same for optimize_mapping — inspect.signature shows constraint_selection param with default None.
    - Test 3 (test_stage_reads_constraint_selection_from_context): Create a StageContext with data["constraint_selection"] = ConstraintSelection(dma_channels=False). Instantiate ConstraintOptimizationAllocationStage (mock list_of_callables). Verify self.constraint_selection.dma_channels is False.
    - Test 4 (test_stage_defaults_constraint_selection_when_absent): Create a StageContext without constraint_selection key. Instantiate the Stage. Verify self.constraint_selection equals ConstraintSelection() (all True).
    - Test 5 (test_scheduler_stores_constraint_selection): Instantiate SteadyStateScheduler with constraint_selection=ConstraintSelection(buffer_descriptors=False). Verify self.constraint_selection.buffer_descriptors is False.
    - Test 6 (test_scheduler_defaults_constraint_selection_when_none): Instantiate SteadyStateScheduler without constraint_selection. Verify self.constraint_selection is None.
    - Test 7 (test_cli_disable_constraints_parsing): Create a ConstraintSelection from a list ["memory_capacity", "dma_channels"] using the pattern: disabled = set(lst); ConstraintSelection(memory_capacity="memory_capacity" not in disabled, ...). Verify memory_capacity=False, dma_channels=False, others True. (This tests the conversion logic that CLI scripts will use.)
  </behavior>
  <action>
    **Step 1 — Write tests first (RED):**
    Create `tests/unit/test_pipeline_threading.py` with the 7 tests above.

    For tests 1-2: use `import inspect; sig = inspect.signature(optimize_allocation_co)` and assert `"constraint_selection"` is in `sig.parameters` and `sig.parameters["constraint_selection"].default is None`.

    For tests 3-4: The Stage constructor requires `list_of_callables` and `ctx`. Create a minimal StageContext with all REQUIRED_FIELDS populated with MagicMock objects:
    ```python
    from unittest.mock import MagicMock
    from stream.stages.context import StageContext
    ctx = StageContext.from_kwargs(
        workload=MagicMock(), accelerator=MagicMock(), mapping=MagicMock(),
        cost_lut=MagicMock(), fusion_splits={}, output_path="/tmp/test",
        backend="ORTOOLS_GSCIP",
    )
    ```
    For test 3, also set `ctx.set(constraint_selection=ConstraintSelection(dma_channels=False))` before constructing the Stage. Use `stage = ConstraintOptimizationAllocationStage([MagicMock()], ctx)`.

    For tests 5-6: SteadyStateScheduler.__init__ requires positional args (workload, accelerator, mapping, fusion_splits, cost_lut). Pass MagicMock() for each. For test 5, pass `constraint_selection=ConstraintSelection(buffer_descriptors=False)`. For test 6, omit constraint_selection entirely.

    For test 7: Pure logic test — no mocks needed. Just test the conversion pattern.

    **Step 2 — Run tests (confirm RED):**
    Tests 1-2 will fail (constraint_selection not yet in signatures). Tests 3-4 will fail (Stage doesn't read it). Test 5 will fail (Scheduler doesn't accept it).

    **Step 3 — Implement (GREEN):**

    **stream/api.py:**
    - Line 11: Change `from stream.opt.solver import GurobiBackend, SolverBackend` to `from stream.opt.solver import ConstraintSelection, GurobiBackend, SolverBackend`
    - `optimize_allocation_co` signature (line 112): Add `constraint_selection: ConstraintSelection | None = None,` as the last parameter before `) -> StreamCostModelEvaluation:`. Keep `# noqa: PLR0913`.
    - `StageContext.from_kwargs(...)` call inside `optimize_allocation_co` (line 161-171): Add `constraint_selection=constraint_selection,` after the `backend=_backend_enum.value,` line.
    - `optimize_mapping` signature (line 189): Add `constraint_selection: ConstraintSelection | None = None,` as the last parameter before `) -> StageContext:`. Keep `# noqa: PLR0913`.
    - `StageContext.from_kwargs(...)` call inside `optimize_mapping` (line 249-264): Add `constraint_selection=constraint_selection,` after the `backend=_backend_enum.value,` line.

    **stream/stages/allocation/constraint_optimization_allocation.py:**
    - Add import: `from stream.opt.solver import ConstraintSelection` (after the existing SteadyStateScheduler import on line 5, ruff will sort).
    - In `__init__` (after line 56 `self.backend: str = ...`): Add:
      ```python
      self.constraint_selection = self.ctx.get("constraint_selection") or ConstraintSelection()
      ```
    - In `find_best_tensor_transfer_allocation` (the SteadyStateScheduler constructor call around line 72-81): Add `constraint_selection=self.constraint_selection,` after `backend=self.backend,`.

    **stream/cost_model/steady_state_scheduler.py:**
    - Add import: `from stream.opt.solver import ConstraintSelection` (ruff will sort).
    - In `__init__` signature (after `backend: str = "ORTOOLS_GSCIP",` on line 65): Add `constraint_selection: ConstraintSelection | None = None,` as the next parameter. Add `# noqa: PLR0913` to the `def __init__(` line if not already present.
    - In `__init__` body (after `self.backend = backend` on line 89): Add `self.constraint_selection = constraint_selection`.
    - In `run()` method, the TTA constructor call (lines 123-135): Add `constraint_selection=self.constraint_selection,` after `backend=self.backend,`.

    **Step 4 — Run tests (confirm GREEN):**
    All 7 tests should pass. Also run full unit suite: `pytest tests/unit/ -x -q`.
  </action>
  <verify>
    <automated>pytest tests/unit/test_pipeline_threading.py -x -q && pytest tests/unit/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - stream/api.py contains `from stream.opt.solver import ConstraintSelection, GurobiBackend, SolverBackend`
    - stream/api.py contains `constraint_selection: ConstraintSelection | None = None` in optimize_allocation_co signature
    - stream/api.py contains `constraint_selection: ConstraintSelection | None = None` in optimize_mapping signature
    - stream/api.py contains `constraint_selection=constraint_selection,` in BOTH StageContext.from_kwargs calls
    - stream/stages/allocation/constraint_optimization_allocation.py contains `from stream.opt.solver import ConstraintSelection`
    - stream/stages/allocation/constraint_optimization_allocation.py contains `self.constraint_selection = self.ctx.get("constraint_selection") or ConstraintSelection()`
    - stream/stages/allocation/constraint_optimization_allocation.py contains `constraint_selection=self.constraint_selection,` in SteadyStateScheduler call
    - stream/cost_model/steady_state_scheduler.py contains `from stream.opt.solver import ConstraintSelection`
    - stream/cost_model/steady_state_scheduler.py contains `constraint_selection: ConstraintSelection | None = None`
    - stream/cost_model/steady_state_scheduler.py contains `self.constraint_selection = constraint_selection`
    - stream/cost_model/steady_state_scheduler.py contains `constraint_selection=self.constraint_selection,` in TransferAndTensorAllocator call
    - tests/unit/test_pipeline_threading.py exists with at least 7 test functions
    - `pytest tests/unit/test_pipeline_threading.py -x -q` exits 0
    - `pytest tests/unit/ -x -q` exits 0 (no regressions)
  </acceptance_criteria>
  <done>
    optimize_allocation_co and optimize_mapping both accept constraint_selection kwarg (defaulting to None) and pass it through StageContext. ConstraintOptimizationAllocationStage reads it from context (defaulting to ConstraintSelection() when absent) and passes it to SteadyStateScheduler. SteadyStateScheduler stores it and passes it to TransferAndTensorAllocator. All 7+ pipeline threading tests pass. Full unit suite green.
  </done>
</task>

</tasks>

<verification>
After task completion, verify the full threading chain:
1. `grep -n "constraint_selection" stream/api.py` — should show the param in both function signatures and both from_kwargs calls
2. `grep -n "constraint_selection" stream/stages/allocation/constraint_optimization_allocation.py` — should show import, ctx.get, and SteadyStateScheduler forwarding
3. `grep -n "constraint_selection" stream/cost_model/steady_state_scheduler.py` — should show import, __init__ param, self assignment, and TTA forwarding
4. `pytest tests/unit/ -x -q` — all tests pass
</verification>

<success_criteria>
- constraint_selection parameter exists on optimize_allocation_co and optimize_mapping with default None
- Parameter threads through StageContext -> Stage -> Scheduler -> TTA (4 hops, all verified by tests)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/06-pipeline-api-surface/06-01-SUMMARY.md`
</output>
